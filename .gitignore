# Per-host config — never committed. Generated by Workflows/Setup.md.
config.json

# Bus identity — a contributor's own agent name, never this repo's business.
# Committing one would ship a live fleet identity to everyone who clones, and
# on a locked host the read-time guard would ignore it anyway.
.claude/ntfy-bus.config.json

# Local credential files in case anyone drops them here by accident
.env
.env.local
*.local.json

# Editor / OS
.DS_Store
.vscode/
.idea/
.claude/settings.local.json
